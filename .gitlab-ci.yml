stages:
  - build

build:
  image: docker:stable
  stage: build
  services:
    - docker:dind
  except:
    refs:
      - tags
  only:
    - master
  tags:
    - shell
  variables:
    GOPATH: $CI_PROJECT_DIR/.go
  retry:
    max: 2
    when: runner_system_failure
#  before_script:
#    - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $CI_REGISTRY
  script:
    - mkdir -p .go
    - docker pull $REGISTRY/$CI_PROJECT_NAME:latest || true
    - docker build . --cache-from $REGISTRY/$CI_PROJECT_NAME:latest
      -t $REGISTRY/$CI_PROJECT_NAME:$CI_PIPELINE_ID
      -t $REGISTRY/$CI_PROJECT_NAME:latest
      --build-arg GITLAB_TOKEN=$GITLAB_TOKEN
      --build-arg GITLAB_DOMAIN=$GITLAB_DOMAIN
      --build-arg RABBIT_HOST=$RABBIT_HOST
    - docker push $REGISTRY/$CI_PROJECT_NAME:$CI_PIPELINE_ID
    - docker push $REGISTRY/$CI_PROJECT_NAME:latest
    - docker image rm $REGISTRY/$CI_PROJECT_NAME:latest
    - docker image rm $REGISTRY/$CI_PROJECT_NAME:$CI_PIPELINE_ID